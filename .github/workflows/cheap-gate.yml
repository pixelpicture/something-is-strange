name: Cheap MVP Gate

on:
  push:
    branches: [ main, feature/mvp-physical-proof ]
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: node scripts/validate.mjs
