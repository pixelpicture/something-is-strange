# PROJECT_STATE

## Active BET
TikTok-Native Game Experiment Factory.

Reference candidate: **Something Is Strange**. Broad GOLD MINE search remains frozen while this BET is physically validated or killed. Budget rule: buy information, not architecture/content volume.

## Authoritative physical baseline — V5 FAIL
Latest independent phone evidence overrides prior proxy PASS claims:
- graphics remained weak;
- Extra Shadow rejected a reasonable tap on the lower visible part of the answer shadow;
- Wrong Light Switch was visually/copy-wise unclear;
- Shadow Ahead did not read as a cast shadow;
- Early Splash rejected a reasonable tap on the upper visible splash;
- Color Theft was understandable;
- pacing had no isolated failure signal;
- there was no compelling reason to continue (no meaningful evaluation/progression/social object).

Therefore: V5 physical comprehension/value FAIL; coded-SVG production visual system REJECT; rectangular answer hotspots REJECT; no-meta `FOUND -> NEXT` sequence REJECT; pacing HOLD/frozen.

## Product reconstruction
Keep the underlying anomaly-detection BET, not the five-puzzle product. Reference Game 01 is a bounded **Perception Challenge**:
`normal expectation -> impossible event -> semantic tap -> reaction/accuracy evaluation -> Perception Score/rank -> next challenge -> session result`.

Only THREE reconstruction rounds before another physical decision:
1. Extra Shadow
2. Early Splash
3. Color Theft

Wrong Light Switch is removed until it independently earns recognizability. Shadow Ahead remains R&D until a real cast-shadow visual reads instantly.

If three high-quality/fair rounds plus meaningful evaluation still do not create spontaneous desire to continue, KILL Something Is Strange and retain Factory V1.

## Factory Architecture V1
Canonical design: `FACTORY_ARCHITECTURE_V1.md`.
Reusable contracts:
- `factory/reference-game-01.json` — data-first reference spec;
- `factory/perception-score-v1.js` — transparent scoring/rank contract;
- `factory/semantic-hit-v1.js` — semantic polygon/mask hit testing with touch expansion;
- `scripts/validate_factory_v1.mjs` — contract/regression gate.

Invariant: a player who understood the answer must never fail because an invisible rectangular hotspot disagrees with the visible semantic object.

## Native implementation — verified state
`factory-native/` is a real Cocos Creator 3.8.8 project:
- engine-independent score/session/semantic-hit/coordinate contracts;
- data-first manifests for Extra Shadow, Early Splash and Color Theft;
- deterministic layered raster asset/evidence generation;
- manifest-faithful before/anomaly/after renderer;
- `LayeredSceneRenderer`, `ReferenceGameController`, `ReferenceGameFlow`, `ResultCardController`;
- `PlayableBootstrap.ts` programmatically creates Canvas, gameplay renderer/controller, HUD, result card and starts the three-round flow;
- `ReferenceGame01.scene` mounts the bootstrap component;
- full native TypeScript source compiles against official `@cocos/creator-types@3.8.8`;
- Cocos 3.x project settings are stored in `factory-native/settings/v2/packages/project.json` with design resolution 1080x1920.

## Real Creator 3.8.8 ByteDance build — PASS
The former vendor-distribution blocker is resolved.

Canonical proof: **Factory Native Contract V1 run #43**, run id `33289714649`, commit `e5fd11b89070e6223e33ff2d3778be6f1457edf3`.

Verified by the real editor/build pipeline:
- official Cocos Creator version: **3.8.8**;
- official package: `https://download.cocos.com/CocosCreator/v3.8.8/CocosCreator-v3.8.8-mac-121518.zip`;
- actual Creator build target: `bytedance-mini-game`;
- build completed successfully with Cocos exit code `36`;
- built output contains `game.json` and ByteDance mini-game runtime files;
- built `src/settings.json` reports **1080x1920** design resolution;
- launch scene is `db://assets/ReferenceGame01.scene`.

This is a **real engine/package build PASS**, not a source-shape or typecheck proxy. It is still NOT evidence of an actual TikTok/ByteDance device/runtime launch.

## CI budget policy
Canonical workflow: `.github/workflows/factory-native-contract.yml`.

Expensive real Creator proof is now **manual-only** via `workflow_dispatch` input `creator_proof=true`.

Budget-fix proof: **run #44**, run id `33290165065`, commit `59160f13c09fb8fb64280993088098db0ef0ec36`:
- cheap Ubuntu `contract`: **SUCCESS**;
- macOS `creator-proof`: **SKIPPED**.

Do not create parallel workflow copies. Do not re-run real Creator proof unless code/settings affecting the build materially change or a physical candidate requires a fresh package.

## Semantic fairness evidence — PASS at artifact level
Human-readable `tap-coverage.png` evidence is generated for each scene using the same semantic geometry/touch-expansion contract used by runtime validation.

Artifact review:
- Extra Shadow: contour-following answer region — PASS;
- Early Splash: contour-following answer region — PASS;
- Color Theft: previous rectangular ball region was rejected; it was replaced with a round 12-point contour aligned to the visible ball — PASS.

Machine coverage still requires >=99.5% visible-answer alpha coverage. Artifact-level semantic fairness is therefore PASS for the current three scenes, but physical touch behavior on the actual runtime remains unverified.

## Visual architecture / quality
No `engine-v5/v6` bespoke coded SVG production scenes. Current native asset contract is ordinary layered raster sprites plus manifests and semantic geometry.

The deterministic Pillow generator/evidence renderer is useful for repeatable machine/human evidence but **is not production-quality art**. Current visual quality remains HOLD. Do not confuse build correctness or semantic fairness with commercial beauty, instant recognizability, fun or TikTok-native appeal.

## Platform correction
Production target remains **Cocos Creator 3.8.8 / TypeScript -> ByteDance/TikTok Mini Games runtime**. Existing HTML/WebView/Android path remains legacy R&D/evidence only. Unity remains a future adapter only for candidates that materially need 3D/physics.

## Evidence policy
Machine CI may PASS state transitions, exceptions, semantic hit coverage, timing, manifests/assets, package integrity, provenance, Cocos API compatibility, real Creator package build and evidence generation.

Machine/assistant proxy review must NOT claim final PASS for beauty, instant recognizability, fun, desire-to-continue, commercial quality, or physical TikTok runtime behavior. Those require real runtime / physical human evidence.

## Kill / throughput policy
Future candidates: one bounded repair for a concrete falsifiable cause; two independent human failures of the same core loop => KILL unless a genuinely different product hypothesis exists; no >3 polished rounds before desire-to-continue is demonstrated.

Post-extraction targets: mechanic proof <=1h autonomous; 3-round slice <=3h when reusing families; machine evidence <=30m; human artifact review <=10m; strong phone candidate within one working day. Second game is the benchmark: if it is not several times faster than Reference Game 01, extraction is incomplete.

## Current gates
- V5 old physical phone slice: **FAIL**
- Rejected SVG/WebView production visual system: **REJECT**
- Rectangular hotspot interaction: **REJECT**
- No-meta five-puzzle product: **REJECT**
- Continuous anomaly mechanic: **BET/HOLD**
- Factory Architecture V1: **FROZEN / IMPLEMENTED TO REFERENCE-SLICE DEPTH**
- Native domain/runtime: **IMPLEMENTED**
- Cocos playable scene/bootstrap: **IMPLEMENTED / CONTRACT PASS**
- Official Cocos 3.8.8 API typecheck: **PASS**
- Real Cocos Creator 3.8.8 ByteDance package build: **PASS**
- Built design resolution 1080x1920: **PASS**
- Semantic hotspot fairness, artifact-level: **PASS**
- Production visual quality: **HOLD**
- Actual TikTok/ByteDance runtime launch on device: **NOT VERIFIED**
- Reconstructed three-round physical-human product gate: **NOT RUN**
- Backend / ads / IAP / content scale: **BLOCKED**
- H1 blind humans: **BLOCKED** until reconstructed physical PASS
- Organic TikTok: **BLOCKED** until H1 PASS

## Next P0
Stop build-plumbing work unless a concrete runtime/build defect appears.

Next P0 is physical/product validation:
1. make the same THREE native scenes visually strong enough to justify a phone test without expanding scope;
2. produce the physical ByteDance/TikTok runtime candidate from the already-proven Creator path;
3. run one real-device loop: `5-second hook -> find anomaly -> semantic tap -> strong reveal -> evaluation/result -> continue`;
4. judge graphics, comprehension, touch fairness, reveal strength, pacing and desire-to-continue separately;
5. decide BET / HOLD / KILL from physical evidence.

Do not return to broad GOLD MINE search, rejected WebView production work, new mechanics, or content scaling before this physical decision.